model Skill {
  id             BigInt           @id @default(autoincrement())
  name           String           @unique
  candidates     CandidateSkill[]
  company_skills CompanySkill[]
  jobs           JobSkill[]
}
