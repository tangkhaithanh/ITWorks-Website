model JobSkill {
  id       BigInt       @id @default(autoincrement())
  job_id   BigInt
  skill_id BigInt
  type     JobSkillType @default(REQUIRED)
  job      Job          @relation(fields: [job_id], references: [id])
  skill    Skill        @relation(fields: [skill_id], references: [id])
}
