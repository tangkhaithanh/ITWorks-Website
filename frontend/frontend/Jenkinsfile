pipeline {
    agent any
    
    environment {
        IMAGE_NAME = 'itworks-frontend'
        CONTAINER_NAME = 'itworks-frontend'
        DOCKER_NETWORK = 'itworks-network'
        
        APP_PORT = '80'
        HTTP_PORT = '5173'
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo '📥 Checking out code from GitHub...'
                checkout scm
            }
        }
        
        stage('Create Docker Network') {
            steps {
                script {
                    echo '🌐 Creating Docker network...'
                    sh """
                        docker network create ${DOCKER_NETWORK} 2>/dev/null || true
                    """
                }
            }
        }
        
        stage('Build Frontend Image') {
            steps {
                script {
                    echo '🔨 Building Frontend Docker image...'
                    dir('frontend/frontend') {
                        def imageTag = "${IMAGE_NAME}:${BUILD_NUMBER}"
                        def imageLatest = "${IMAGE_NAME}:latest"
                        
                        sh """
                            docker build -t ${imageTag} -t ${imageLatest} .
                        """
                        
                        echo "✅ Frontend image built: ${imageTag}"
                    }
                }
            }
        }
        
        stage('Test') {
            steps {
                script {
                    echo '🧪 Running tests...'
                    dir('frontend/frontend') {
                        echo '⚠️  Tests skipped (uncomment to enable)'
                    }
                }
            }
        }
        
        stage('Stop Old Containers') {
            steps {
                script {
                    echo '🛑 Stopping old containers if exists...'
                    sh """
                        docker stop ${CONTAINER_NAME} || true
                        docker rm ${CONTAINER_NAME} || true
                    """
                }
            }
        }
        
        stage('Deploy Frontend') {
            steps {
                script {
                    echo '🚀 Deploying Frontend container...'
                    sh """
                        docker run -d \\
                            --name ${CONTAINER_NAME} \\
                            --restart unless-stopped \\
                            -p ${HTTP_PORT}:80 \\
                            --network ${DOCKER_NETWORK} \\
                            ${IMAGE_NAME}:${BUILD_NUMBER}
                    """
                }
            }
        }
        
        stage('Health Check') {
            steps {
                script {
                    echo '🏥 Waiting for application to start...'
                    sleep(time: 5, unit: 'SECONDS')
                    
                    sh """
                        if docker ps | grep -q ${CONTAINER_NAME}; then
                            echo "✅ Frontend container is running"
                        else
                            echo "❌ Container failed to start"
                            docker logs ${CONTAINER_NAME}
                            exit 1
                        fi
                    """
                    
                    sh """
                        sleep 3
                        curl -f http://localhost:${HTTP_PORT}/health || echo "⚠️  Health check endpoint not available"
                    """
                    
                    sh """
                        curl -f http://localhost:${HTTP_PORT}/ || echo "⚠️  Homepage check failed"
                    """
                }
            }
        }
        
        stage('Cleanup') {
            steps {
                script {
                    echo '🧹 Cleaning up old images...'
                    sh """
                        docker images ${IMAGE_NAME} --format '{{.ID}} {{.Tag}}' | \\
                        grep -v latest | \\
                        sort -k2 -n -r | \\
                        tail -n +6 | \\
                        awk '{print \$1}' | \\
                        xargs -r docker rmi || true
                    """
                }
            }
        }
    }
    
    post {
        success {
            echo '✅ Pipeline completed successfully!'
        }
        failure {
            echo '❌ Pipeline failed!'
            script {
                sh """
                    docker logs ${CONTAINER_NAME} || true
                """
            }
        }
        always {
            echo '📋 Pipeline finished'
        }
    }
}