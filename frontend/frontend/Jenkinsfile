pipeline {
    agent any
    
    environment {
        // Tên image và container
        IMAGE_NAME = 'itworks-frontend'
        CONTAINER_NAME = 'itworks-frontend'
        NGINX_IMAGE_NAME = 'itworks-nginx'
        NGINX_CONTAINER_NAME = 'itworks-nginx'
        DOCKER_NETWORK = 'itworks-network'
        
        // Port mapping
        APP_PORT = '80'
        NGINX_HTTP_PORT = '80'
        NGINX_HTTPS_PORT = '443'
        
        // Domain và SSL
        DOMAIN = 'itworks.dpdns.org'
        SSL_EMAIL = 'admin@itworks.dpdns.org'
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo '📥 Checking out code from GitHub...'
                checkout scm
            }
        }
        
        stage('Create Docker Network') {
            steps {
                script {
                    echo '🌐 Creating Docker network...'
                    sh """
                        docker network create ${DOCKER_NETWORK} 2>/dev/null || true
                    """
                }
            }
        }
        
        stage('Build Frontend Image') {
            steps {
                script {
                    echo '🔨 Building Frontend Docker image...'
                    dir('frontend/frontend') {
                        // Build frontend image với tag
                        def imageTag = "${IMAGE_NAME}:${BUILD_NUMBER}"
                        def imageLatest = "${IMAGE_NAME}:latest"
                        
                        sh """
                            docker build -t ${imageTag} -t ${imageLatest} .
                        """
                        
                        echo "✅ Frontend image built: ${imageTag}"
                    }
                }
            }
        }
        
        stage('Build Nginx Image') {
            steps {
                script {
                    echo '🔨 Building Nginx reverse proxy image...'
                    dir('frontend/frontend/nginx') {
                        // Build nginx image với tag
                        def nginxImageTag = "${NGINX_IMAGE_NAME}:${BUILD_NUMBER}"
                        def nginxImageLatest = "${NGINX_IMAGE_NAME}:latest"
                        
                        sh """
                            docker build -t ${nginxImageTag} -t ${nginxImageLatest} .
                        """
                        
                        echo "✅ Nginx image built: ${nginxImageTag}"
                    }
                }
            }
        }
        
        stage('Test') {
            steps {
                script {
                    echo '🧪 Running tests...'
                    dir('frontend/frontend') {
                        // Chạy tests trong container (optional)
                        // sh 'docker run --rm ${IMAGE_NAME}:${BUILD_NUMBER} npm test'
                        echo '⚠️  Tests skipped (uncomment to enable)'
                    }
                }
            }
        }
        
        stage('Stop Old Containers') {
            steps {
                script {
                    echo '🛑 Stopping old containers if exists...'
                    sh """
                        docker stop ${CONTAINER_NAME} ${NGINX_CONTAINER_NAME} || true
                        docker rm ${CONTAINER_NAME} ${NGINX_CONTAINER_NAME} || true
                    """
                }
            }
        }
        
        stage('Deploy Frontend') {
            steps {
                script {
                    echo '🚀 Deploying Frontend container...'
                    // Frontend không cần expose port ra ngoài, chỉ cần trong Docker network
                    // Nginx sẽ proxy đến container này
                    sh """
                        docker run -d \\
                            --name ${CONTAINER_NAME} \\
                            --restart unless-stopped \\
                            --network ${DOCKER_NETWORK} \\
                            ${IMAGE_NAME}:${BUILD_NUMBER}
                    """
                }
            }
        }
        
        stage('Setup SSL') {
            steps {
                script {
                    echo '🔒 Setting up SSL certificate...'
                    
                    // Kiểm tra quyền sudo trước
                    def hasSudo = sh(
                        script: "sudo -n true 2>&1 && echo 'yes' || echo 'no'",
                        returnStdout: true
                    ).trim()
                    
                    if (hasSudo == 'no') {
                        echo "⚠️  Jenkins user không có quyền sudo, bỏ qua SSL setup"
                        echo "ℹ️  Để enable SSL, cần cấu hình sudo cho Jenkins user:"
                        echo "   sudo visudo"
                        echo "   Thêm dòng: jenkins ALL=(ALL) NOPASSWD: /usr/bin/apt-get, /usr/bin/certbot, /usr/bin/test"
                        env.SSL_EXISTS = 'not_exists'
                    } else {
                        // Kiểm tra SSL certificate đã tồn tại chưa
                        def sslExists = sh(
                            script: "sudo test -f /etc/letsencrypt/live/${DOMAIN}/fullchain.pem && echo 'exists' || echo 'not_exists'",
                            returnStdout: true
                        ).trim()
                        
                        if (sslExists == 'not_exists') {
                            echo "📝 SSL certificate chưa tồn tại, đang tạo mới..."
                            
                            // Đảm bảo port 80 free (stop Nginx container nếu đang chạy)
                            sh """
                                docker stop ${NGINX_CONTAINER_NAME} || true
                                docker rm ${NGINX_CONTAINER_NAME} || true
                            """
                            
                            // Cài đặt certbot và lấy certificate
                            sh """
                                sudo apt-get update -qq || true
                                sudo apt-get install -y -qq certbot || true
                                sudo certbot certonly --standalone \\
                                    -d ${DOMAIN} \\
                                    -d www.${DOMAIN} \\
                                    --non-interactive \\
                                    --agree-tos \\
                                    --email ${SSL_EMAIL} \\
                                    --preferred-challenges http || echo "⚠️  SSL certificate creation failed, will use HTTP"
                            """
                            
                            // Kiểm tra lại SSL
                            sslExists = sh(
                                script: "sudo test -f /etc/letsencrypt/live/${DOMAIN}/fullchain.pem && echo 'exists' || echo 'not_exists'",
                                returnStdout: true
                            ).trim()
                        }
                        
                        env.SSL_EXISTS = sslExists
                    }
                    
                    dir('frontend/frontend/nginx') {
                        def sslExists = env.SSL_EXISTS ?: 'not_exists'
                        
                        if (sslExists == 'exists') {
                            echo "✅ SSL certificate đã tồn tại, enabling HTTPS..."
                            
                            // Enable HTTPS trong nginx.conf
                            sh """
                                # Backup nginx.conf
                                cp nginx.conf nginx.conf.backup || true
                                
                                # Uncomment HTTPS server block
                                sed -i 's/# server {/server {/g' nginx.conf
                                sed -i 's/#     listen 443/    listen 443/g' nginx.conf
                                sed -i 's/#     server_name/    server_name/g' nginx.conf
                                sed -i 's/#     ssl_certificate/    ssl_certificate/g' nginx.conf
                                sed -i 's/#     ssl_certificate_key/    ssl_certificate_key/g' nginx.conf
                                sed -i 's/#     ssl_protocols/    ssl_protocols/g' nginx.conf
                                sed -i 's/#     ssl_ciphers/    ssl_ciphers/g' nginx.conf
                                sed -i 's/#     ssl_prefer_server_ciphers/    ssl_prefer_server_ciphers/g' nginx.conf
                                sed -i 's/#     ssl_session_cache/    ssl_session_cache/g' nginx.conf
                                sed -i 's/#     ssl_session_timeout/    ssl_session_timeout/g' nginx.conf
                                sed -i 's/#     add_header/    add_header/g' nginx.conf
                                sed -i 's/#     gzip/    gzip/g' nginx.conf
                                sed -i 's/#     proxy_set_header/    proxy_set_header/g' nginx.conf
                                sed -i 's/#     proxy_connect_timeout/    proxy_connect_timeout/g' nginx.conf
                                sed -i 's/#     proxy_send_timeout/    proxy_send_timeout/g' nginx.conf
                                sed -i 's/#     proxy_read_timeout/    proxy_read_timeout/g' nginx.conf
                                sed -i 's/#     location/    location/g' nginx.conf
                                sed -i 's/#         proxy_pass/        proxy_pass/g' nginx.conf
                                sed -i 's/#         proxy_http_version/        proxy_http_version/g' nginx.conf
                                sed -i 's/#         proxy_set_header/        proxy_set_header/g' nginx.conf
                                sed -i 's/#         expires/        expires/g' nginx.conf
                                sed -i 's/#         add_header/        add_header/g' nginx.conf
                                sed -i 's/# }/}/g' nginx.conf
                                
                                # Enable HTTP to HTTPS redirect
                                sed -i 's/# return 301 https/return 301 https/g' nginx.conf
                            """
                            
                            // Rebuild Nginx image với HTTPS enabled
                            sh """
                                docker build -t ${NGINX_IMAGE_NAME}:${BUILD_NUMBER} -t ${NGINX_IMAGE_NAME}:latest .
                            """
                            
                            echo "✅ Nginx image đã được rebuild với HTTPS enabled"
                        } else {
                            echo "⚠️  SSL certificate chưa được tạo, sẽ dùng HTTP only"
                        }
                    }
                }
            }
        }
        
        stage('Deploy Nginx') {
            steps {
                script {
                    echo '🚀 Deploying Nginx reverse proxy container...'
                    
                    // Kiểm tra SSL có tồn tại không (dùng biến từ stage trước)
                    def sslExists = env.SSL_EXISTS ?: sh(
                        script: "sudo -n test -f /etc/letsencrypt/live/${DOMAIN}/fullchain.pem 2>/dev/null && echo 'exists' || echo 'not_exists'",
                        returnStdout: true
                    ).trim()
                    
                    // Build docker run command
                    if (sslExists == 'exists') {
                        echo "✅ Deploying với SSL certificates"
                        sh """
                            docker run -d \\
                                --name ${NGINX_CONTAINER_NAME} \\
                                --restart unless-stopped \\
                                -p ${NGINX_HTTP_PORT}:80 \\
                                -p ${NGINX_HTTPS_PORT}:443 \\
                                -v /etc/letsencrypt:/etc/letsencrypt:ro \\
                                --network ${DOCKER_NETWORK} \\
                                ${NGINX_IMAGE_NAME}:${BUILD_NUMBER}
                        """
                    } else {
                        echo "⚠️  Deploying không có SSL (HTTP only)"
                        sh """
                            docker run -d \\
                                --name ${NGINX_CONTAINER_NAME} \\
                                --restart unless-stopped \\
                                -p ${NGINX_HTTP_PORT}:80 \\
                                -p ${NGINX_HTTPS_PORT}:443 \\
                                --network ${DOCKER_NETWORK} \\
                                ${NGINX_IMAGE_NAME}:${BUILD_NUMBER}
                        """
                    }
                }
            }
        }
        
        stage('Health Check') {
            steps {
                script {
                    echo '🏥 Waiting for application to start...'
                    sleep(time: 5, unit: 'SECONDS')
                    
                    // Kiểm tra container có chạy không
                    sh """
                        if docker ps | grep -q ${CONTAINER_NAME}; then
                            echo "✅ Container is running"
                        else
                            echo "❌ Container failed to start"
                            docker logs ${CONTAINER_NAME}
                            exit 1
                        fi
                    """
                    
                    // Kiểm tra Nginx container
                    sh """
                        if docker ps | grep -q ${NGINX_CONTAINER_NAME}; then
                            echo "✅ Nginx container is running"
                        else
                            echo "❌ Nginx container failed to start"
                            docker logs ${NGINX_CONTAINER_NAME} || true
                            exit 1
                        fi
                    """
                    
                    // Kiểm tra qua Nginx reverse proxy (HTTP)
                    sh """
                        sleep 3
                        curl -f http://localhost/health || echo "⚠️  Health check endpoint not available"
                    """
                    
                    // Kiểm tra trang chủ qua Nginx (HTTP)
                    sh """
                        curl -f http://localhost/ || echo "⚠️  Homepage check failed"
                    """
                    
                    // Kiểm tra HTTPS nếu có SSL (dùng biến từ stage trước)
                    def sslExists = env.SSL_EXISTS ?: sh(
                        script: "sudo -n test -f /etc/letsencrypt/live/${DOMAIN}/fullchain.pem 2>/dev/null && echo 'exists' || echo 'not_exists'",
                        returnStdout: true
                    ).trim()
                    
                    if (sslExists == 'exists') {
                        sh """
                            echo "🔒 Testing HTTPS..."
                            curl -k -f https://localhost/health || echo "⚠️  HTTPS health check failed"
                        """
                    }
                }
            }
        }
        
        stage('Setup SSL Auto-Renewal') {
            steps {
                script {
                    echo '🔄 Setting up SSL auto-renewal...'
                    
                    // Kiểm tra SSL có tồn tại không (dùng biến từ stage trước)
                    def sslExists = env.SSL_EXISTS ?: sh(
                        script: "sudo -n test -f /etc/letsencrypt/live/${DOMAIN}/fullchain.pem 2>/dev/null && echo 'exists' || echo 'not_exists'",
                        returnStdout: true
                    ).trim()
                    
                    if (sslExists == 'exists') {
                        // Kiểm tra quyền sudo trước
                        def hasSudo = sh(
                            script: "sudo -n true 2>&1 && echo 'yes' || echo 'no'",
                            returnStdout: true
                        ).trim()
                        
                        if (hasSudo == 'yes') {
                            // Setup cron job cho auto-renewal
                            sh """
                                # Tạo cron job để renew SSL và restart Nginx container
                                (sudo crontab -l 2>/dev/null | grep -v "certbot renew.*${NGINX_CONTAINER_NAME}"; \\
                                 echo "0 0 * * * sudo certbot renew --quiet --deploy-hook \\"docker restart ${NGINX_CONTAINER_NAME}\\"") | sudo crontab -
                                
                                echo "✅ SSL auto-renewal đã được setup"
                            """
                        } else {
                            echo "⚠️  Không có quyền sudo, bỏ qua auto-renewal setup"
                        }
                    } else {
                        echo "⚠️  SSL chưa được setup, bỏ qua auto-renewal"
                    }
                }
            }
        }
        
        stage('Cleanup') {
            steps {
                script {
                    echo '🧹 Cleaning up old images...'
                    // Xóa images cũ (giữ lại 5 images gần nhất) cho cả frontend và nginx
                    sh """
                        docker images ${IMAGE_NAME} --format '{{.ID}} {{.Tag}}' | \\
                        grep -v latest | \\
                        sort -k2 -n -r | \\
                        tail -n +6 | \\
                        awk '{print \$1}' | \\
                        xargs -r docker rmi || true
                        
                        docker images ${NGINX_IMAGE_NAME} --format '{{.ID}} {{.Tag}}' | \\
                        grep -v latest | \\
                        sort -k2 -n -r | \\
                        tail -n +6 | \\
                        awk '{print \$1}' | \\
                        xargs -r docker rmi || true
                    """
                }
            }
        }
    }
    
    post {
        success {
            echo '✅ Pipeline completed successfully!'
            // Có thể thêm notification ở đây (email, Slack, etc.)
        }
        failure {
            echo '❌ Pipeline failed!'
            script {
                // Log container logs nếu fail
                sh """
                    docker logs ${CONTAINER_NAME} || true
                """
            }
        }
        always {
            echo '📋 Pipeline finished'
            // Cleanup workspace nếu cần
            // cleanWs()
        }
    }
}

